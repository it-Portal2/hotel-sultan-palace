rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all read/write access for development and testing
    // This is temporary for development - implement proper auth later
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
